---
// Component Imports
import MainHeader from "@components/MainHead.astro";
import Header from "@layouts/Header.astro";
import CardContent from "@layouts/CardContent.astro";
import Footer from "@layouts/Footer.astro";

import Corona from "@content/cards/Corona.astro";

const title = `Corona an der Isle of LAN`;
const description = `Corona an der Isle of LAN`;
const keywords = ["Corona Informationen", "Isle of LAN", "Sulgen"];
const type = "website";
const canonicalURL = Astro.href;
---

<html lang="de">
  <head>
    <MainHeader {title} {description} keywords={keywords.join(", ")} image="" {type} {canonicalURL}
    />
  </head>
  <body>
    <Header />
    <CardContent>
      <Corona />
    </CardContent>
    <Footer />
  </body>
</html>
